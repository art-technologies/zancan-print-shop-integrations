# zancan print shop integrations

We have quite simple Webpack setup because it's much easier
to bundle `wagmi` and `web3modal` this way.

There are also two directories:
- `dist` has outer-produced libraries
- `lib` has li
